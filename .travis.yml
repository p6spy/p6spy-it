language: java

jdk:
  - oraclejdk7

before_install:
 - "sudo apt-get update -qq"

install: mvn -B dependency:resolve dependency:resolve-plugins
 

before_script:
 - groovy script/travis/generateMavenSettingsForIT.groovy
  
script: mvn -version; mvn -B clean install -P tomcat-7.x --settings ~/.m2/itSettings.xml

notifications:
 email: false
#   - p6spy-developers@googlegroups.com