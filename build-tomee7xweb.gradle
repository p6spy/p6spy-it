configurations {
  tomee7xwebTestRuntime { extendsFrom testRuntime }
}

ext {
  tomee = "7.0.2"
}

dependencies {
    tomee7xwebTestRuntime "org.apache.tomee:arquillian-tomee-remote:$tomee"
}

String rootDir = "$buildDir/unpack/apache-tomcat"
String binDir = rootDir + "/bin"

task unpackContainer(type: Copy) {
  from { zipTree( downloadFile("http://repo.maven.apache.org/maven2/org/apache/tomee/apache-tomee/$tomee/apache-tomee-$tomee-webprofile.zip"))}
  into 'build/unpack'
  doLast {
    ant.rename(src: "build/unpack/apache-tomee-webprofile-$tomee", dest: 'build/unpack/apache-tomcat')
  }
}

apply from: "build-tomcat-common.gradle"
